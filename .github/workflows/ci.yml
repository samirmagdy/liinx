name: CI / Production Quality Gate

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  validate-and-test:
    name: Lint, Build & Comprehensive Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Rebuild native dependencies for Linux runner
        run: npm rebuild better-sqlite3

      - name: Static Type Check (tsc)
        run: npm run typecheck

      - name: Static Code Quality & Linting (ESLint)
        run: npm run lint

      - name: Architectural guardrails
        run: npm run architecture:check

      - name: Build Production Frontend & Server Bundles
        run: npm run build

      - name: Execute Full Test Suite & Enforce Coverage (Vitest)
        env:
          NODE_ENV: test
          JWT_SECRET: test_jwt_secret_ci_key_32_characters_minimum
          INTEGRATION_ENCRYPTION_KEY: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
          STRIPE_WEBHOOK_SECRET: whsec_test_secret_for_ci_suite
        run: npm run test:coverage

      - name: Verify Bundle Integrity
        run: |
          test -f dist/index.html || (echo "dist/index.html is missing!" && exit 1)
          test -f dist-server/server.js || (echo "dist-server/server.js is missing!" && exit 1)
          echo "Production frontend and server assets successfully compiled and validated."
